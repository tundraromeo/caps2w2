# ===============================================
# ENGUIO INVENTORY SYSTEM - ENVIRONMENT TEMPLATE
# ===============================================
# Copy this file to create your environment configuration:
#   - Copy to .env.local for frontend (Next.js)
#   - Copy to .env for backend (PHP)

# ===============================================
# FRONTEND CONFIGURATION (.env.local)
# ===============================================
# Next.js API Base URL (must start with NEXT_PUBLIC_)
NEXT_PUBLIC_API_BASE_URL=http://localhost/caps2e2/Api

# ===============================================
# BACKEND CONFIGURATION (.env)
# ===============================================
# Database Configuration
DB_HOST=localhost
DB_DATABASE=enguio2
DB_USERNAME=root
DB_PASSWORD=
DB_PORT=3306
DB_CHARSET=utf8mb4

# API Base URL (for internal backend-to-backend calls)
API_BASE_URL=http://localhost/caps2e2/Api

# Application Environment
APP_ENV=development`n`n# CORS Configuration - comma-separated list of allowed origins`nCORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000,http://127.0.0.1:3001

# ===============================================
# PRODUCTION DEPLOYMENT NOTES
# ===============================================
# 1. Update NEXT_PUBLIC_API_BASE_URL to your domain
#    Example: https://yourdomain.com/Api
# 2. Set strong DB_PASSWORD for production
# 3. Change APP_ENV to production
# 4. Never commit .env or .env.local to git
